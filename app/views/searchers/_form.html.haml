= simple_form_for @searcher, :html => { :class => 'form-horizontal' } do |f|
  = f.input :user_id
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), searchers_path, :class => 'btn'
